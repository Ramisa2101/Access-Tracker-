# ---- Django Core ----
Django>=5.0,<6.0

# ---- Database ----
psycopg2-binary>=2.9

# ---- Environment Variables ----
python-dotenv>=1.0

# ---- Image & File Handling ----
Pillow>=10.0   # for handling photo uploads (visitor images)

# ---- Printing & PDF (optional but useful) ----
reportlab>=4.0   # for generating printable stickers or passes

# ---- Frontend Helpers ----
django-tailwind>=3.8   # if you plan to use Tailwind CSS for styling

# ---- Security & Deployment (optional, future use) ----
gunicorn>=21.2     # for production server
whitenoise>=6.6    # for serving static files in production

# ---- Admin & Debug Tools ----
django-extensions>=3.2
django-debug-toolbar>=4.3

# Development utilities
alembic==1.14.0          # DB migrations
pytest==8.3.3            # Testing framework
httpx==0.27.2            # Async HTTP client for testing APIs
black==24.8.0            # Code formatter
flake8==7.1.1            # Linter
isort==5.13.2            # Import sorter
pre-commit==4.0.1        # Pre-commit hooks for code quality
elasticsearch<9.0.0,>=8.0.0
requests
databases[postgresql]
psycopg2-binary
sqlmodel

# djangorestframework
# django-cors-headers
waitress==3.0.2
sslserver
rest